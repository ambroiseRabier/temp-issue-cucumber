import { Before, Given, Then, When } from 'cucumber';

Given('the word "hello"', function() {
  // cb()
  // return 1;
});

When('the word has been inputted', function() {
  // this.incrementBy(number);
});

Then('see the word "hello"', function() {
  // expect(1).toBe(1)
  // expect(this.variable).to.eql(number);
});
